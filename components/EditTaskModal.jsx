export const EditTaskModal = ({ show, onClose, onSave, task }) => {

    const values = {
        show,
        onClose,
        onSave,
        task,
    }

    return (
        <h1>Sono modale</h1>
    )


}